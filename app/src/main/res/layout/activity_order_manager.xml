<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:paddingBottom="8dp"
    android:paddingHorizontal="8dp"
    android:layout_height="match_parent"
    tools:context=".activities.OrderManagerActivity">
    <androidx.appcompat.widget.Toolbar
        android:background="@color/white"
        android:id="@+id/toolbar_order_manager"
        android:layout_width="match_parent"
        android:layout_height="?attr/actionBarSize"/>
    <androidx.recyclerview.widget.RecyclerView
        android:layout_marginBottom="4dp"
        android:layout_above="@id/layout_unit_price"
        android:layout_below="@id/toolbar_order_manager"
        android:id="@+id/rv_user_order"
        android:padding="8dp"
        android:layout_width="match_parent"
        android:layout_height="match_parent"/>
    <LinearLayout
        android:id="@+id/layout_unit_price"
        android:orientation="horizontal"
        android:layout_alignParentBottom="true"
        android:layout_width="match_parent"
        android:layout_height="wrap_content">
        <TextView
            android:textStyle="bold"
            android:textSize="22sp"
            android:text="Tổng tiền: "
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"/>
        <TextView
            android:id="@+id/tv_unit_price_orders"
            android:layout_marginStart="8dp"
            android:textColor="@color/orange"
            android:textSize="25sp"
            android:textStyle="bold"
            android:text="23.000$"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"/>
    </LinearLayout>
</RelativeLayout>